# Render deployment: full inference stack on Python 3.11 (matches the shap 0.46
# / numpy 2.2 retrain, so the bundled pickles deserialize correctly). Render
# keeps the process warm, so the SHAP explainer load at boot is fine.
numpy==2.2.6
scipy==1.14.1
scikit-learn==1.5.2
lightgbm==4.5.0
pandas==2.2.3
shap==0.46.0
numba==0.61.2
joblib==1.4.2
pyyaml==6.0.2

fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
